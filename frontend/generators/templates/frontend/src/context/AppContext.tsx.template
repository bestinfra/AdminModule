import React, { createContext, useContext, useEffect, useState } from 'react';

interface AppContextType {
    isDarkMode: boolean;
    isSidebarCollapsed: boolean;
    toggleTheme: () => void;
    toggleSidebar: () => void;
}

const AppContext = createContext<AppContextType>({
    isDarkMode: false,
    isSidebarCollapsed: false,
    toggleTheme: () => {},
    toggleSidebar: () => {},
});

<<<<<<< HEAD:src/context/AppContext.tsx
export { AppContext };

export const AppProvider: React.FC<{ children: React.ReactNode }> = ({
    children,
}) => {
=======
export const AppProvider = ({ children }: { children: React.ReactNode }) => {
>>>>>>> be6a00465516c0f621bf0dfd1b2252475400ac8d:generators/templates/frontend/src/context/AppContext.tsx.template
    const [isDarkMode, setIsDarkMode] = useState(() => {
        const savedTheme = localStorage.getItem('theme');
        const prefersDark = window.matchMedia(
            '(prefers-color-scheme: dark)'
        ).matches;
        return savedTheme === 'dark' || (!savedTheme && prefersDark);
    });

    const [isSidebarCollapsed, setIsSidebarCollapsed] = useState(() => {
        const savedState = localStorage.getItem('sidebarCollapsed');
        return savedState ? JSON.parse(savedState) : false;
    });

    useEffect(() => {
        if (isDarkMode) {
            document.documentElement.classList.add('dark');
            localStorage.setItem('theme', 'dark');
        } else {
            document.documentElement.classList.remove('dark');
            localStorage.setItem('theme', 'light');
        }
    }, [isDarkMode]);

    useEffect(() => {
        localStorage.setItem(
            'sidebarCollapsed',
            JSON.stringify(isSidebarCollapsed)
        );
    }, [isSidebarCollapsed]);

    const toggleTheme = () => {
        setIsDarkMode((prev) => !prev);
    };

    const toggleSidebar = () => {
        setIsSidebarCollapsed((prev: boolean) => !prev);
    };

    return (
        <AppContext.Provider
            value={{
                isDarkMode,
                isSidebarCollapsed,
                toggleTheme,
                toggleSidebar,
            }}>
            {children}
        </AppContext.Provider>
    );
};

export const useApp = (): AppContextType => {
    // Check for global context first (for federated components)
    const globalContext = (window as any).__FEDERATED_APP_CONTEXT__;
    if (globalContext) {
        try {
            const context = useContext(globalContext);
            if (context) {
                return context as AppContextType;
            }
        } catch (error) {
            console.warn('useApp: Error accessing global context:', error);
        }
    }
<<<<<<< HEAD:src/context/AppContext.tsx
    
    // Fallback to local context
    try {
        const context = useContext(AppContext);
        if (context !== undefined) {
            return context;
        }
    } catch (error) {
        console.warn('useApp: Error accessing local context:', error);
    }
    
    // Provide fallback implementation for federated modules
    console.warn('useApp: AppProvider context not available, using fallback implementation');
    
    // Create a fallback implementation that uses localStorage directly
    const fallbackContext: AppContextType = {
        isDarkMode: (() => {
            try {
                const savedTheme = localStorage.getItem('theme');
                const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
                return savedTheme === 'dark' || (!savedTheme && prefersDark);
            } catch (error) {
                console.warn('useApp: Error reading theme from localStorage:', error);
                return false;
            }
        })(),
        isSidebarCollapsed: (() => {
            try {
                const savedState = localStorage.getItem('sidebarCollapsed');
                return savedState ? JSON.parse(savedState) : false;
            } catch (error) {
                console.warn('useApp: Error reading sidebar state from localStorage:', error);
                return false;
            }
        })(),
        toggleTheme: () => {
            try {
                const currentTheme = localStorage.getItem('theme');
                const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
                localStorage.setItem('theme', newTheme);
                
                if (newTheme === 'dark') {
                    document.documentElement.classList.add('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                }
                
                // Force re-render by dispatching a custom event
                window.dispatchEvent(new CustomEvent('themeChanged', { detail: { theme: newTheme } }));
            } catch (error) {
                console.warn('useApp: Error toggling theme:', error);
            }
        },
        toggleSidebar: () => {
            try {
                const currentState = localStorage.getItem('sidebarCollapsed');
                const newState = !(currentState ? JSON.parse(currentState) : false);
                localStorage.setItem('sidebarCollapsed', JSON.stringify(newState));
                
                // Force re-render by dispatching a custom event
                window.dispatchEvent(new CustomEvent('sidebarToggled', { detail: { collapsed: newState } }));
            } catch (error) {
                console.warn('useApp: Error toggling sidebar:', error);
            }
        }
    };
    
    return fallbackContext;
};
=======
    return context;
}; 
>>>>>>> be6a00465516c0f621bf0dfd1b2252475400ac8d:generators/templates/frontend/src/context/AppContext.tsx.template
